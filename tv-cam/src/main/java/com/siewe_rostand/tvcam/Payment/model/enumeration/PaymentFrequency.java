package com.siewe_rostand.tvcam.Payment.model.enumeration;

public enum PaymentFrequency {
    MONTHLY, QUARTERLY, SEMI_ANNUALLY, ANNUALLY;
    public static final PaymentFrequency DEFAULT = MONTHLY;
}
